"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.updateLibPackageNpmScope = void 0;
const devkit_1 = require("@nrwl/devkit");
function updateLibPackageNpmScope(host, options) {
    let pkgName = options.importPath;
    const scopeParts = options.importPath.split('/');
    if (scopeParts.length > 2) {
        const prefix = scopeParts.shift();
        const name = scopeParts.join('-');
        pkgName = `${prefix}/${name}`;
    }
    return devkit_1.updateJson(host, `${options.projectRoot}/package.json`, (json) => {
        json.name = pkgName;
        return json;
    });
}
exports.updateLibPackageNpmScope = updateLibPackageNpmScope;
//# sourceMappingURL=update-lib-package-npm-scope.js.map